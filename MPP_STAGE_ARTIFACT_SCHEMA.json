{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["stage", "task_id", "content"],
  "properties": {
    "stage": {"type": "integer", "minimum": 1, "maximum": 10},
    "task_id": {"type": "string", "minLength": 1},
    "content": {"type": "object"}
  }
}
